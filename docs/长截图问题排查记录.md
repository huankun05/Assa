# 长截图（滚动拼接）功能问题排查记录

> 记录 Xiyue 长截图功能在 2026-09-07 ~ 09-09 调试过程中遇到的问题、根因与修复。
> 范围：渲染端 `resources/capture.js` + `resources/capture.css`、主进程自动滚动 IPC、编辑器窗口渲染。
> 关联文档：`docs/汐月Electron版现状.md`
> 版本标记：r19 ~ r49（一轮修复记一个 r 号，便于回溯）。

## 一、功能现状

长截图采用「主屏可见 + 自动匀速滚动 + 静止抓帧 + 帧-累积匹配 + DP 最小能量缝拼接」的桌面主流方案
（与 ShareX / eSearch 同源，非录屏式）。数据流：

```
主进程捕获（koffi GDI BitBlt+GetDIBits）
  → 渲染端 lsMatchScroll 帧-accum 匹配
  → lsAppendRows 整数 1:1 拼装 + DP 最小能量缝
  → 编辑器预览 → cropLongShotFull 1:1 导出 PNG
```

截至 r38，三类最显眼症状（画质模糊 / 接缝横线 / 蓝色边框残留）代码层均已修复；
拼接几何在 r26 高选区与 dwarf 矮选区真实帧 E2E 上总高精确命中。r38 之后尚未经用户实测闭环。

**r49 现状更新（2026-09-09）**：滚轮注入已从方案A（mouse_event 依赖鼠标在选区洞内透传）升级为
方案B（PostMessage WM_MOUSEWHEEL 直达选区下方 Chromium 的 `Chrome_RenderWidgetHostHWND` 子窗口）——
彻底修好「鼠标移出选区不滚 / 抢鼠标」焦点依赖，并修复 DPI 逻辑坐标→物理坐标换算 bug。
但用户实测复发两大症状：**① 长图里「大部分内容重复」；② 清晰度仍不行（局部发虚/重影）**。
两者同源——滚轮推进节奏与「静止抓帧」脱节（详见 3.7 节）。

## 二、问题清单（速查）

| # | 症状（用户反复报） | 真因 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 画质模糊 | 编辑器窗口 setBounds resize 触发合成器栅格尺度失效（--disable-gpu 下 raster 停留 1.0、再 1.5× 输出=整窗发虚）；非导出、非运动模糊 | r27-r29 | 已修，待实测闭环 |
| 2 | 接缝横线 | 早期 6→12 行线性羽化带在文字处发虚（r30a 实证） | r32（DP 最小能量缝） | 已修 |
| 3 | 拼接错位/重复 | 矮选区首格初始 delta 预估失败（r37 算出 -19→首格仅滚 95→重叠 72% 内容重复）；周期内容（聊天/列表/代码）锁到内容周期假谷被 seed 接受 | r37（初始 delta=-60 + 0.5×帧高软偏好） | 已修 |
| 4 | 拼接全盘乱 | BIN 死引用→ReferenceError 整帧丢失（r26）；GDI 静止帧 e0<0.3 旧阈值漏过→假谷（r28）；采样 stepX=9 翻转真假谷（r26） | r26 / r28 | 已修 |
| 5 | 蓝色边框残留 | .capture-mask-frame 描边未隐藏（r34）/ drawMask 泄漏白色手柄（r35）/ frame border 仍定义（r36） | r34-r36 | 已修 |
| 6 | 零拼接（快滚） | 重叠下限 60 行把快速甩滚（重叠 26~41 行）全拒（r20） | r20 / r22 | 已修 |
| 7 | 大部分内容重复 + 清晰度仍差（r49 后复发） | 滚轮 delta 经 Chromium 平滑滚动异步推进、位移漂移：首格实滚仅 120px（帧高 500~600 → 重叠 76~80%），后续 120/250/420 跳变→重复囤积；soft_frame 仅落盘不丢弃→软帧混入→发虚 | r50 尝试（过紧致退化）→ r50b 回退 | **待重新设计**（见 3.8） |

## 三、详细排查

### 3.1 画质模糊（问题 #1）

**现象**：用户多次截图反馈长截图编辑器内整图发虚，但导出 PNG 本身是清晰的。

**根因（r27 实锤）**：用 CDP `Page.captureScreenshot` 抓取编辑器窗口合成输出，与源图逐像素对比（mse=0 判 1:1）。
- 若编辑态发生 setBounds 缩放（如 1708×920 → 946×920），渲染输出 lap_var 永久掉到原图 31%；
- 软件合成（--disable-gpu / SwiftShader）下 resize 后栅格尺度停留 1.0、再放大输出 → 整窗发虚。
- 用户自 r22 编辑器存在以来看到的「模糊」全部是这个，导出 PNG 实际清晰。

**附带澄清**：此前「运动模糊」「遮罩压暗」等理论在真实帧上被证伪（同内容配对误差极低、帧间对齐整数精确）；
顶部「横线/半透明带」是聊天窗 SVG 卡片自身渐变设计，非拼接瑕疵。

**修复**：
- r27：尺寸已满足 → move-only 保持原尺寸；需缩放 → 屏外改尺寸 200ms 后挪回；闭环自愈（主进程 900ms 后实测 lap_var 比值 <0.7 → hide/show 重建合成表面）。
- r28：sharp-guard 守卫固定取图顶部居中，聊天窗顶部纯背景 lap=0 导致守卫整体跳过 → 改为扫描 5×N 候选取 lap 最大区。
- r29：缩小 resize 也是触发器 → 窗口已 ≥ 目标尺寸走 move-only；编辑态 canvas 加 `image-rendering:pixelated`；自愈从 hide/show 改为 `webContents.setZoomFactor(1.001)` 抖动后回 1.0。

**验证**：CDP 抓窗口合成输出逐像素对比，mse=0 判 1:1。

### 3.2 接缝横线（问题 #2）

**现象**：拼接处出现一道明显横线 / 半透明带。

**根因**：早期用 6→12 行线性羽化带（alpha 渐变）做接缝融合。全景拼接文献（PicLab / Springer）明确反对宽线性羽化带——
低频亮度差发虚、高频边缘出重影，即这道横线成因。

**修复（r32）**：删掉羽化带，改用内容感知 DP 最小能量缝——在名义缝 ±2 行内逐列用 DP 找使「缝上/缝下」像素差最小的垂直线，
沿缝硬切 + 仅 1px 淡化（无宽渐变带）。无横线、无发虚。

### 3.3 拼接错位 / 重复（问题 #3）

**现象**：拼接后内容重叠、错位、重复出现。

**根因（r37 / r38 用真实帧 manifest 取证）**：
- 第一格位移过大/过小污染速度基准：固定初始 `delta=-60` 在用户 App 滚动系数下，某些选区首格滚 509 行（重叠仅 31 行）或滚 95 行（重叠 72% 内容重复）。
- 周期内容假谷：聊天 / 列表 / 代码块等内容呈周期性，seed 模式（accept<2）完全跳过速度先验，把内容周期假谷（如 160）当真位移接受，后续错位累积。

**修复**：
- r37：`isSeeding` 收紧（有可信基准即退出 seed 启用速度先验）、`velBound=max(20, lastGoodS*0.3)`、强谷豁免在矮选区不再豁免速度约束、初始 delta 自适应预估。
- r38：初始 delta 固定回 -60（r37 的 0.055×帧高预估在部分 App 系数下算出 -19 致首格仅滚 95）；seeding 候选加向 0.5×帧高软偏好，防周期内容锁假谷。

### 3.4 拼接全盘乱（问题 #4，历史）

**根因**：
- r26：r25 废弃 `const BIN` 但 best-guess 细搜仍引用 `bestSB*BIN` → 死引用 ReferenceError 整帧丢失。
- r26：采样列子集 stepX=w/160≈9px 在浅误差曲线上翻转真假谷（真实帧 f0005：stepX=9 下伪谷 68 < 真谷 422）。
- r28：静止判据 `e0<0.3` 是视频帧时代遗产，GDI 同内容重抓 e0 实测 87~1061 → 静止帧漏过 → 粗搜找假谷。

**修复**：BIN 死引用修为 bestSB±4；全分辨率 stepX=2 扫描 + 谷检测 + top-4 细搜；
GDI 静止判据改为 `e0<2500 && frameDiff<=0.5` → skip 不计拒链；effR 速度加权仲裁替换裸 evalR 终审。

### 3.5 蓝色边框残留（问题 #5，UI bug）

**现象**：长截图编辑器内四周出现蓝色带白边方框 + 白色 resize 手柄。

**根因**：`.capture-mask-frame`（选区高亮框）在长截图编辑态未被正确隐藏；
`enterLongShotEditor` 设 `display:none` 后 `drawMask()` 经 `layoutHandles(true)` 又把手柄显示出来；frame 本身仍定义蓝色 border/outline。

**修复**：
- r34：`body.is-longshot-edit .capture-mask-frame { display: none !important; }`。
- r35：`drawMask()` 的 `layoutHandles(...)` 加 `&& !document.body.classList.contains('is-longshot-edit')`，编辑态永不显示手柄。
- r36：`.capture-mask-frame` 的 `border:none; outline:none;`，`body.is-longshot .capture-mask-frame` 同样去蓝边。普通 / 长截图都不再有蓝框。

### 3.6 零拼接（快滚，问题 #6，历史）

**现象**：快速甩滚时 matches:[] 零接受，整段丢失。

**根因（r20 实锤）**：用户快速甩滚单帧位移 388~405px，重叠仅 26~41 行，被重叠下限 normalMinOverlap=60 全拒。

**修复**：r20 种子级接受 + 深谷豁免重叠下限；r21-r23 改「自动匀速步进制滚动」（单格滚轮 → lsWaitQuiet 判静止 → 抓帧），
每格位移恒定 ≈50% 帧高、重叠恒足，从源头不拒拼。

### 3.7 r49 复测复发：大部分内容重复 + 清晰度仍差（问题 #7）

**现象（用户实测 2026-09-09 13:46 / 13:48 复现，本轮代码是 r49 方案B PostMessage 滚轮）**：
长截图结果里「大部分内容是重复的」，且局部发虚 / 重影。不是偶发——采集到两段真实会话 manifest。

**取证（本机 `%LOCALAPPDATA%\Temp\xiyue-ls-debug\`）**：

```
会话A  selX427 selY288 selW854 selH336 dpr1.5  → 物理帧高 = 336×1.5 = 504
  frames=9，matches=[{2,s:120},{3,s:250},{4,s:250.01},{5,s:250},{8,s:290},{9,s:258.15}]
  → 首格(s:120) 重叠 = (504-120)/504 = 76%；稳态 ~250 → 重叠 ~50%
会话B  selX596 selY284 selW802 selH400 dpr1.5  → 物理帧高 = 400×1.5 = 600
  frames=5，matches=[{2,s:120},{3,s:420.01},{4,s:433}]
  → 首格(s:120) 重叠 = (600-120)/600 = 80%；随后 s 跳到 420~433（重叠仅 30%）→ 位移跳变 3.5×
```

**根因分析（两条链，同源于「滚轮推进 ≠ 静止抓帧」）**：

**(A) 「大部分重复」= 实际滚动位移过小（重叠远超设计）**
- 设计目标 `lsAutoTargetFrac = 0.5`（`capture.js` L4382）→ 每格期望推 50% 新内容、重叠 50%。
- 实测首格位移仅 **120px**，而帧高 500~600px → **重叠 76~80%**，每帧只引入 20~24% 新内容——
  「几乎整屏被前一帧覆盖」就是用户看到的"大部分重复"。
- 位移不稳定（会话B：120→420→433 跳变 3.5×；frames 数 > matches 数 = 有被拒帧）→ 推进断档 / 忽快忽慢。
- 为何？方案B 的 `delta` 经 Chromium **平滑滚动（Smooth Scroll）异步推进**：delta 被平滑系数放大带惯性、
  且滚动是异步落定的。`lsWaitQuiet`（每 100ms 抓 GDI 灰度、连续 3 次 diff≤0.05 判静止 + 60ms）在平滑滚动
  末段（亚像素缓动、或内容相似度高如纯背景/小字渐隐时）误判"已静止"→ 抓帧时机漂移 → 单帧实际位移
  不可预测（120~433 随机）。位移小 → 重叠大 → 内容重复囤积。

**(B) 「清晰度不行」= 软帧混入拼接**
- 证据：`ls_1788932930521_1/2/3_acc_soft_frame.png` 落盘 → 这些帧 `lap < lsSharpBase×0.6` 且 retries≥3，被标记为软帧。
- 关键缺陷（`capture.js` L4950）：软帧判定后**仅 `lsDebugDump(...,'soft_frame')` 落盘做诊断，并不丢弃 / 重抓**，
  软帧照样进 `lsAppendRows` 拼接 → 滚动动画中途帧 = 上下半透明重影 → 局部发虚。
- 平滑滚动时 GDI 抓到的"静止"在亚像素层仍缓动，sharp-gate 对"下一行文字正在淡入"的帧误判为清晰。

**结论**：r49 已把「鼠标位置 / 焦点依赖」彻底修好（post-wheel 全部 ok=1、全程无需抢前台焦点），
但核心矛盾转向「**滚轮推进节奏 ⇄ 静止抓帧时机** 的脱节」——这是两症状共同的根。位移漂移既是"重复"的因，
也放大了软帧概率。

**修复方向（未实施，建议下一步）**：
1. **首格大步长校准**：首格用固定大步（如 delta=-180~-240 甩出近半屏）先测 ratio，再进稳态，
   从源头规避首格 s=120 的 76~80% 重叠囤积。
2. **静止判定收紧**：`lsWaitQuiet` 的 GDI diff 阈值 0.05 下调至 ~0.02，或连续判 3~4 次 + 额外 settle 30~60ms；
   对 Chromium 平滑滚动尤其关键。
3. **软帧改丢弃重抓**：soft_frame 判定后整帧丢弃并补发一次滚轮重抓（代价仅多一步），杜绝软帧混入——而非仅落盘。
4. **位移多态仲裁**：normal(≈120) 与 best-guess(≈420) 交替时，信任速度先验锁定单一基线，防首格小位移污染后续。
5. **目标重叠下探备选**：位移稳定后可把 `lsAutoTargetFrac` 0.5 → 0.65~0.7（target=0.3~0.4×帧高），提高每帧新内容占比，进一步减少重复观感。

### 3.8 r50 退化与 r50b 回退（2026-09-09 15:21）——对 #7 的认知修正

**现象（r50 首测，会话 `ls_1788938457044`）**：`frames:1`、`matches:[{i:1,t:9712,s:552}]`、4 张 soft_frame 落盘。
即 9.7 秒只拼成一帧、且该帧位移 552px（帧高 579，重叠仅 27px ≈ 5% → 内容大量跳过）；滚动几乎不推进。

**取证软帧**（`ls_1788938457044_1_acc_soft_frame.png`）：**整图均匀发虚 / 降采样感，无任何方向性运动拖影、无淡入重叠**。
这是「静态显示缩放 / 目标窗口降采样」的真容，**不是**滚动动画中途帧。

**根因（r50 自身两处破坏性改动 + 一个认知错误）**：
1. 静止判定过紧（0.02 / 连续4次 / settle100ms）：含轻微持续动画的页面几乎永不判静止 → 每步吃满 2500ms 超时 → 极慢。
2. soft_frame 丢弃重抓：sharp-gate（lap_var<0.6×EMA）在本内容上**误触发**（正常静帧被判软），丢弃后循环再注入一次滚轮
   → 内容在未被捕获时持续滚走 → 位移累积成 552px 大跳、大量内容未拍即跳 → "移动截图没实现"。
3. **认知错误**：soft_frame 未必是动画伪影。sharp-gate 的 lap_var 判据把「真实内容就是发虚（显示缩放）」当成本可修复的
   采集抖动 → 不该丢弃。r49 时代「仅落盘、照常拼接」反而正确保真。

**r50b 修复**：回退静止判定（0.05/连续3/settle60）、回退 soft_frame（仅落盘、照常拼接），保留自适应 delta clamp。

**对 #7 的再定位**：内容重复与发虚的根源不是"静止判定不够严"，而是**平滑滚动的位移时机不稳定**。
推进方向应从「收紧静止判定(牺牲速度)」改为「**用匹配器实测位移 s 做稳定步长反馈**」——即让每次注入的滚轮量
根据上一格实测位移修正，使每格位移恒定逼近 0.5×帧高（r50 已保留 clamp 的雏形，仍需按步长=目标直接收敛，而非比例微调）。

## 四、验证方法（沉淀）

- 语法：`node --check resources/capture.js`
- 渲染端加载 / 按钮绑定：`_vm_load_check.js`（真实执行 capture.js，16/16）
- 匹配器回归：`_matcher_check.js`（11/11）
- UI 视觉测试：`_ls_edit_visual_test.js`（Playwright + 真实 Chromium，注入 electron/process 垫片，进入编辑态截图 + 计算样式断言 + 人工看图）
- 拼接几何 E2E：提取生产 `lsMatchScroll` 真实源码 + 用户真实会话 `ls_<t>_manifest.json` 帧喂入，比对帧-帧 SSD 真值与接受值
- 清晰度：CDP `Page.captureScreenshot` 抓窗口合成输出，与源图逐像素对比

## 五、残留风险

1. 矮选区重叠下限 60 行（≈11% 帧高）偏紧，极低噪声页仍可能拒拼 / 锁错。
2. 周期内容假谷在极低噪声页仍可能被 seed 接受。
3. r38 首格固定 delta=-60 在 App 滚动系数差异大时可能仍需调。
4. **【r50b 已回退·恢复可用】滚轮推进 ⇄ 静止抓帧脱节**：r50 曾把静止判定 0.02/连续4次/settle100ms + soft_frame 丢弃重抓，
   实测 15:21 会话退化（见 3.8）→ **r50b 已回退这两处**，仅保留自适应 delta clamp。
   - 静止判定回退原值（0.05 / 连续3次 / settle60ms，轮询 100ms、步进停顿 200ms 不变）→ 恢复速度；
   - soft_frame 回退为「仅落盘诊断、照常拼接」——取证软帧实为整图均匀发虚（静态显示缩放/降采样），
     非滚动动画中途帧，sharp-gate 误触发；丢弃只会造成重滚位移漂移与丢内容。

**下一步（r50b：已回退 r50 的 2/3 两处破坏性改动，仅保留 4 的 clamp；真正解决 #7 仍需按 3.7 重新设计）**：
1. 首格大步长校准测 ratio（未做）；2. 静止判定收紧（r50 做但过紧致 9.7s/步，已回退）；3. soft_frame 丢弃（r50 做但误伤正常静帧，已回退）；
4. 位移多态仲裁（r50 用 delta clamp 部分近似，保留）；5. 目标重叠下探（未做）。
推进方向改为：**先保证可用与速度（本步达成），再用更可靠特征（如帧间位移统计/内容真值）设计「稳定步长」而非收紧静止判定**。

取证沿用 `%LOCALAPPDATA%\Temp\xiyue-ls-debug\` 下 `ls_<时间戳>_manifest.json` + `*_acc_soft_frame.png`；
备选兜底：UI Automation 读 ScrollPattern 实测滚动分数，直接修正 delta 系数。
