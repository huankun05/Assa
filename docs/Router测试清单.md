# Router 回归句集与用户测试清单（B6）

> **版本**：2026-09-11 · 与当前 Router 实现对齐（规则匹配已用 16 条短语逐条冒烟）  
> **环境**：Windows · `F:\Work\Create\Assa\Assa`  
> **入口**：仅 **`/chat/stream`**（大面板 AI 聊天 / 岛内对话）。旧 `/chat` **不走规则**。  
> **角色模式**：出厂「中立助手」即可测；与权限档无关。

---

## 0. 测试前 2 分钟准备

```powershell
cd F:\Work\Create\Assa\Assa
npm run dev
```

1. 等侧车起来（终端有 `[assa-agent]` 或 `/health` 正常）。  
2. 打开灵动岛 → 进入 AI 对话（流式）。  
3. 准备：可调亮度的显示器；需要时先放一首音乐（测媒体）。

查看日志（另开终端）：

```powershell
# 路由日志（data/ 已 gitignore，只在本机）
Get-Content F:\Work\Create\Assa\Assa\data\route_log.jsonl -Tail 30
```

---

## A. 自动化基线（你测之前我已跑过）

| 套件 | 结果 |
|---|---|
| Python `agent.tests.test_router_rules` | **9 passed** |
| 短语对照（清单 B 全部输入） | 16/16 路由正确 |
| `assaToolSchema` | 14 passed |
| 插件改名后全量 `npm test` | 1783 passed |

---

## B. 手动冒烟（按顺序勾）

### B1 · 音量

| # | 输入 | 期望 |
|---|---|---|
| 1 | `音量50` | 音量 ≈50%；「好的，音量已经调到 50% 了。」 |
| 2 | `把音量调到30` | ≈30% |
| 3 | `声音调到80` | ≈80% |
| 4 | `音量40` → `音量50` → `音量60` | 依次变化；**无** LLM 长篇 |
| 5 | `静音` | 系统静音；「好，已经静音了。」 |
| 6 | `取消静音` | 恢复声音；「声音已经恢复了。」 |

### B2 · 亮度 / 媒体 / 打开应用

| # | 输入 | 期望 |
|---|---|---|
| 7 | `亮度60` | 亮度变化；有回执 |
| 8 | 放歌时 `暂停` | 暂停；「好了，播放状态已切换。」 |
| 9 | `播放` | 播放/切换 |
| 10 | `下一首` | 切歌；「切到下一首了。」（无 SMTC 会话可能无效，看有无回执） |
| 11 | `上一首` | 回上一首 |
| 12 | `启动记事本` | 弹出记事本；「正在打开 记事本。」 |
| 13 | `打开微信` | 尝试打开；**失败也应有明确回执**，不是 LLM 长聊 |

### B3 · 未命中 → 本地 LLM

| # | 输入 | 期望 |
|---|---|---|
| 14 | `你好` | 正常对话（非音量模板） |
| 15 | `帮我把下载文件夹的 PDF 归档` | 较慢的 LLM/工具规划；**不是**秒回规则话术 |

### B4 · 路由日志

| # | 期望 |
|---|---|
| 16 | 每句有 JSON 行；`tier` 为 `rule` 或 `local_llm` |
| 17 | `音量50` → `rule_id: volume.set`，`args.level: 50` |
| 18 | `静音` → `rule_id: volume.mute` |
| 19 | `下一首` → `rule_id: media.next` |
| 20 | `启动记事本` → `rule_id: sys.launch`，`args.app: notepad` |
| 21 | 有 `request_id` |

### B5 · 主进程审计（可选）

搜索 `assa-tools.log`（Electron `userData/logs/`）：

| # | 期望 |
|---|---|
| 22 | `volume.set` / `sys.launch` 成功行；新版含 `requestId` |

### B6 · 边界

| # | 输入 | 期望 |
|---|---|---|
| 23 | `音量250` | **不调系统**；走 LLM |
| 24 | `音量`（无数字） | 不调系统；走 LLM |

---

## C. 已知限制（这些**不算失败**）

| 项 | 说明 |
|---|---|
| `/chat` 非流式 | 不走规则 |
| 「百分之三十」 | 中文数字不解析 → LLM |
| `打开微信` 若未装/英文名不对 | 可能失败回执，属预期 |
| 无音乐时切歌 | SMTC 无会话可能无效 |
| 纯工具角色模式 | **设置 UI 未做**，B15 已从必测移除 |
| 权限卡/主动关心/分类器 | 未上线，不在本轮范围 |
| `data/route_log.jsonl` | 在 gitignore 的 `data/` 下，仅本机 |

---

## D. 失败时请给我

1. 输入原文  
2. 回执文字/截图  
3. `route_log.jsonl` 最后几行  
4. 控制台 Python `[server]` / 工具相关报错  

---

## E. 测完勾选

- [ ] B1 音量+静音 ×6  
- [ ] B2 亮度/媒体/打开应用 ×7  
- [ ] B3 未命中 ×2  
- [ ] B4 日志 ×6  
- [ ] B5 审计（可选）  
- [ ] B6 边界 ×2  
