# 验收检查清单（统一打勾）

> 用法：自上而下勾选。不通过的项把现象/截图发我。  
> 日期基线：2026-09-12 · 提交含 `3b918ae`（AI 完善）及之后  
> 应用请用最新 `npm run dev` 重启后的进程测试。

---

## A. 岛交互（已验收过，回归）

- [x] A1 鼠标靠近岛 → **悬停展开**
- [x] A2 岛上 **右键** → 约 3 秒隐藏 → **淡入**回来
- [x] A3 让路期间/回来瞬间不会立刻又弹开挡住标签
- [x] A4 右键后 3 秒内能点到后面的浏览器标签

## B. 角色 / 头像

- [x] B1 Hover 汐月页显示少女头像（非恐龙、非线稿）
- [x] B2 切换状态时表情/图会变（开心 / 思考等）
- [ ] B3 岛内 Agent 条为少女图，不是恐龙
- [ ] B4 回答完成后显示偏开心；出错偏困惑

## C. 设置 · AI 信任与审计（M-A1）

- [ ] C1 设置 → 软件设置 → **隐私与安全 · AI 信任与审计** 能打开
- [ ] C2 能切换 L0–L3 四档，详情文字随档变化
- [ ] C3 改完等级后 **不用重启**，再触发一次工具类请求行为有变化（或日志有记录）
- [ ] C4 **工具审计记录** 有条目或显示「暂无」不报错
- [ ] C5 点「刷新」审计列表会更新

## D. 设置 · 记忆（M-A3）

- [ ] D1 同页 **汐月记忆** 能打开
- [ ] D2 聊过几句后列表出现内容，或空态文案正常
- [ ] D3 记忆离线/侧车未起时显示空，**不崩 UI**
- [ ] D4 单条「删除」后该条消失
- [ ] D5 「清空记忆」后列表变空

## E. 对话 / 语音 / 情绪（M-A2 / M-A4）

- [ ] E1 文字对话：岛内或大面板能流式出字
- [ ] E2 有回答后能听到 TTS（或静音环境确认开关）
- [ ] E3 语音热键开麦 → 说完自动停 → 进入对话
- [ ] E4 **开麦时若 TTS 在播，声音会被掐断**（语音打断）
- [ ] E5 回答结束后 hover 表情可能不是恒为「开心」（情绪微调，非必现）
- [ ] E6 音量50 等闭集指令仍走规则秒回（Router 回归）

## F. 工具开关与通行证（M-A5+）

- [ ] F1 默认应 **不能** 自动开网页工具

## F. 浏览器工具（M-A5）

- [ ] F1 默认应 **不能** 自动开网页工具（模型不暴露 browser.*）
- [ ] F2 设置页可开关 **浏览器自动化**；开启并重启会话后工具可用（仍受确认）
- [ ] F3 **会话通行证**开关存在；开启后非删除/非命令确认类可自动过；删除/命令仍要点允许

## G. 未做项（应仍为「未实现」，不必当 bug）

- [ ] G1 唤醒词仍不可用（缺 openwakeword 依赖与模型，未做半成品）
- [ ] G2 CosyVoice 需自备 HTTP 服务并设 `ASSA_COSYVOICE_URL`；未配置则仍走 Kokoro

## H. 工程自测（开发机可选）

- [ ] H1 `python agent/tests/test_p0a_security.py` → 7/7  
- [ ] H2 `python agent/tests/test_router_rules.py` → 9/9  
- [ ] H3 无 i18n JSON 语法错误启动报错  

## I. 托盘重启（dev soft-restart）

- [x] I1 托盘「重启灵动岛」→ 右下角系统通知标题为 **汐月正在重新启动**（dev 头像仍可能是 Electron，属预期）
- [x] I2 岛短暂消失后约 1–2s 自动回来（同一 vite 会话，端口不整段重开）
- [x] I3 重启过程 **无** 命令行/终端闪窗
- [x] I4 重启后 `data/soft-restart.flag` **不存在**（已被 electron-vite 消费删除）
- [ ] I5 设置里点「重启灵动岛」按钮行为与托盘一致
- [ ] I6 **打包安装后**（`npm run package`）系统通知头为 **汐月 + 应用图标**（AUMID=`com.assa.app` 与 appId 对齐；dev 下仍显示 Electron 属正常）

---

## J. 负一屏（Expanded 总览 · 2026-09-16 接入）

- [ ] J1 从 hover 点展开 → 底部导航点出现 **总览**，默认可落在总览
- [ ] J2 总览中间有时钟（日期/农历/宜忌），左右为可配卡片（待办/快捷等）
- [ ] J3 底部点可切到 工具 / 翻译；总览在展开布局设置里可排序显隐
- [ ] J4 总览**不再出现相册轮播**卡片选项
- [ ] J5 设置 → 外观 → 展开布局 中可见「总览」项

## 通过标准建议

- **必过**：A 全部、C1–C5、E1–E3、E6、F1、I1–I4  
- **抽测**：B、D、E4–E5、I5  
- **已知未做**：G 不用勾完成  

## 有问题时请提供

1. 复现步骤  
2. 设置页路径或命令  
3. `Get-Content F:\Work\Create\Assa\assa-dev2.log -Tail 50`  
4. 若侧车异常：控制台 `[assa-agent]` 行  
